# ---------------------------------------------------------------------------
# Core ML / inference
# ---------------------------------------------------------------------------
transformers[torch]==5.14.1
torch>=2.11.0,<2.12.0
accelerate==1.14.0             # required at runtime by transformers.Trainer
datasets==5.0.0
huggingface-hub==1.23.0
sentence-transformers==5.6.0
vllm==0.23.0; sys_platform != "darwin"   # GPU/CUDA only — not installable on macOS

# ---------------------------------------------------------------------------
# LLM APIs
# ---------------------------------------------------------------------------
google-genai==2.8.0
anthropic==0.119.0
openai==2.48.0
jinja2==3.1.6                  # prompt templating (src/llms/llm_client.py)

# ---------------------------------------------------------------------------
# Annotation
# ---------------------------------------------------------------------------
argilla>=2.8,<3.0

# ---------------------------------------------------------------------------
# Training / experiment tracking
# ---------------------------------------------------------------------------
wandb==0.28.0

# ---------------------------------------------------------------------------
# Evaluation
# ---------------------------------------------------------------------------
scikit-learn==1.9.0

# ---------------------------------------------------------------------------
# General utilities
# ---------------------------------------------------------------------------
json-repair==0.61.7
gradio==6.20.0
art==6.5
overrides>=7.7.0,<8.0.0
pprintpp==0.4.0
colorama==0.4.6
termcolor==3.2.0
rich==15.0.0
python-dotenv==1.2.2
fire==0.7.1
orjson==3.11.9
tqdm==4.68.4
yasbd-lib==0.13.1

# ---------------------------------------------------------------------------
# Geotaxonomy
# ---------------------------------------------------------------------------
geopandas==1.1.3

# ---------------------------------------------------------------------------
# Database connectors and cloud platform libraries
# ---------------------------------------------------------------------------
google-cloud-aiplatform==1.158.0
google-cloud-storage==3.13.0
cloud-sql-python-connector[pymysql]==1.20.3
sqlalchemy==2.0.49
psycopg2-binary==2.9.12

# ---------------------------------------------------------------------------
# Dev
# ---------------------------------------------------------------------------
black>=26.1.0,<27.0.0
ruff>=0.14.14,<0.15.0
pytest>=9.1.0,<10.0.0

# ---------------------------------------------------------------------------
# Optional GPU-only extras; install manually on a Linux/CUDA training box. 
# Needed for legacy/unused code:
# src/train/train_unsloth.py,
# src/train/train_gliner.py, src/models/custom_gliner.py,
# scripts/location/extractors.py, scripts/legacy/*,
# and scripts/data/risk_factors/*.py plotting scripts.
# ---------------------------------------------------------------------------
# unsloth==2026.5.7
# trl
# gliner
# gliner2>=1.2.6,<1.3.0
# spacy
# semantic-text-splitter
# fastcoref
# av
# matplotlib
